/**
 * @file postcss.config.js
 * @description Configuración PostCSS con Tailwind y autoprefixer.
 * @responsibilities Procesar CSS para Tailwind.
 * @dependencies tailwindcss, autoprefixer.
 */
module.exports = {
  plugins: {
    tailwindcss: {}, // plugin tailwind
    autoprefixer: {}, // plugin autoprefixer
  },
};
